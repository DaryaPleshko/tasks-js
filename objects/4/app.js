// На входе у вас объект, которые вы придумываете сами. Необходимо вывести все четные элементы значения объекта.

let user = {
    name: 'Dasha',
    age: 18,
    1: 27,
};
for (let num in user) {
    if (!isNaN(user[key])) {
        if (user[num] % 2 === 0) console.log(user[num]);
    }
};


