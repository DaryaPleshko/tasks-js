//Вычислите сумму массива целых чисел статичного массива используя рекурсию

// const checkArray = (array) => {
//     let newArray;
//     for (let i = 0; i < array.length; i++) (Number.isInteger(array[i])) ? newArray += array[i] : null;

//     if (array.length === 0) return 0;
//     else return checkArray(array - 1);
// }
// console.log(checkArray([32, 54.4, 23.5, 75, 45]));